services:
  - type: web
    name: mercado-express-cp4
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
